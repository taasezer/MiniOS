\
    ; paging.asm - (placeholder) In many GRUB setups paging is already enabled; full builder is long.
    BITS 64
    GLOBAL paging_enable
    SECTION .text
    paging_enable:
        ; For safety in this prototype, do nothing.
        ret
